from setuptools import setup, find_packages

setup(
    name='GeoAttachmentSeeker',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add any other dependencies here
    ],
)
